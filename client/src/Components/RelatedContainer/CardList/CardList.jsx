import React from "react"
import ProductCard from "./ProductCard/ProductCard.jsx"

const CardList = () => {
  // accepts a list of products
  // accepts a Card Component
  // accepts an action button React Node for the card (e.g. <button>Like</button>)
  // handles the logic for scrolling through cards left/right

  return (
    <div className="productCard">
      <p>under construction</p>
    </div>
  )
}

export default CardList
