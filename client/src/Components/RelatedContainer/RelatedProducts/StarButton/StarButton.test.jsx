import React from "react"
import { screen, render, fireEvent } from "../../../../test-utils.jsx"
import RelatedProducts from "./RelatedProducts.jsx"
// import { ProductsContext } from '../../../../contexts/ProductsContext.jsx';
// import sampleProduct from 'sampleProduct';

describe("StartButton", () => {
  // test('renders the comparison when clicking the action button', () => {
  //   const { container } = render(<RelatedProducts />);
  //   console.log(container.querySelector("button.actionBtn"))
  //   const ActionBtn
  //   fireEvent.click();
  //   setTimeout(() => {
  //     expect(screen.getByRole('figure')).toBeInTheDocument();
  //     expect(screen.getByText(props.description)).toBeInTheDocument();
  //   }, 0);
  //   fireEvent.mouseLeave(screen.getByText(props.name));
  //   setTimeout(() => {
  //     expect(screen.getByRole('figure')).not.toBeInTheDocument();
  //   }, 0);
  // });
})
